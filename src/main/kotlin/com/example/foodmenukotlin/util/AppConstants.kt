package com.example.foodmenukotlin.util

interface AppConstants {
    companion object {
        val DEFAULT_PAGE_NUMBER = "0"
        val DEFAULT_PAGE_SIZE = "30"

        val MAX_PAGE_SIZE = 50
    }
}
